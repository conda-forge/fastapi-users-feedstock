{% set name = "fastapi-users" %}
{% set version = "14.0.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: c7d39035fc2eaacfae6122c3b29965f7dab0d83123b1c4ae92d20d963aa47fb8

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - hatchling
    - hatch-regex-commit
    - pip
    - python {{ python_min }}
  run:
    - python >={{ python_min }}
    - fastapi >=0.65.2
    - pwdlib ==0.2.1
    - email-validator >=1.1.0,<2.4
    - pyjwt ==2.10.1
    - python-multipart ==0.0.20
    - makefun >=1.11.2,<2.0.0
    # For pwdlib[argon2, bcrypt]
    - bcrypt >=4.1.2,<5
    - argon2-cffi >=23.1.0,<24
    # For pyjwt[crypto]
    - cryptography >=3.4.0

test:
  imports:
    - fastapi_users
    - fastapi_users.authentication
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/fastapi-users/fastapi-users
  summary: Ready-to-use and customizable users management for FastAPI.
  doc_url: https://fastapi-users.github.io/fastapi-users/
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
    - davidbrochart
